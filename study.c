#include <stdio.h>

int main()
{
	/*
	char ch;
	ch = 'A';
	while ( ch <= 'Z')
	{
		printf( "%c", ch );
		ch++;
	}
	return 0;
	*/
	char ch;
	for ( ch = 'A'; ch <= 'Z'; ch++)
	{
		printf( "%c", ch );
	}
	return 0;
}